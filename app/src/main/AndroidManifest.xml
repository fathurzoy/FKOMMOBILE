<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fkommobile">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/fkommobile"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/fkommobile_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DosenActivity"></activity>
        <activity android:name=".TentangActivity" />
        <activity android:name=".MahasiswaActivity" />
        <activity android:name=".MataKuliahActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>